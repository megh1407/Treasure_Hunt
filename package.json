{
  "name": "core-quest-finder",
  "version": "1.0.0",
  "private": true,
  "description": "Core Quest Finder — 3D Virtual Campus Treasure Hunt (Levels 1–10)",
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run preview --prefix frontend",
    "test:rc": "node scratch/test_phase20_release_candidate.js",
    "test:phase21": "node scratch/test_phase21_deployment_readiness.js"
  }
}
